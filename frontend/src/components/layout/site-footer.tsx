export function SiteFooter() {
  return (
    <footer className="border-t border-white/10 bg-surface">
      <div className="mx-auto flex w-full max-w-7xl flex-col gap-3 px-4 py-8 text-sm text-muted-foreground sm:px-6 md:flex-row md:items-center md:justify-between lg:px-8">
        <p className="font-heading uppercase tracking-normal text-foreground">
          Eid Cricket Fest
        </p>
        <p>Live scoring, fixtures, standings, and tournament history.</p>
      </div>
    </footer>
  );
}
